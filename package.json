{
  "name": "node24-ci-guard-complete",
  "version": "1.0.0",
  "description": "Node 24 CI Guard - complete with template, tests, and CI",
  "main": "src/index.ts",
  "bin": {
    "node24-ci-guard": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "prepublishOnly": "npm run build && npm test"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "fs-extra": "^11.2.0",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.19.41",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "files": [
    "src",
    "templates",
    "dist"
  ],
  "engines": {
    "node": ">=20"
  }
}
